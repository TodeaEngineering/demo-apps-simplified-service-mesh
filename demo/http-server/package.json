{
  "name": "kcd-kl-2026-http-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tiny HTTP echo server for the mesh demo (no runtime deps)",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
